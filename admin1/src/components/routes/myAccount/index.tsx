import useGetAdminList from "@/hooks/pagesHooks/useGetAdminList";
import { StyledMainCon, StyledTitle } from "@/styles/styled/reusableStyled";
import React, { useEffect } from "react";

const MyAccount = () => {

  return (
    <StyledMainCon>
      <StyledTitle>My Account</StyledTitle>
    </StyledMainCon>
  );
};

export default MyAccount;
